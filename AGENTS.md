# 프로젝트 규칙

## 라운지

이 프로젝트는 여러 AI 클라이언트가 동시에 작업한다. `.lounge/` 가 공유
기록 공간이다.

세션을 시작하면 가장 먼저 `lounge_join` 을 호출한다. 브리핑에 미확인
항목이 있으면 요약을 먼저 읽고, 내 작업과 관련 있는 것만 본문을 연다.

세션을 끝낼 때 `lounge_leave` 로 다음 세션에 넘길 메모를 남긴다.

상세 규약은 `skills/lounge/SKILL.md` 를 읽는다.

라운지 파일을 손으로 만들거나 고치지 않는다. 도구만 쓴다.

## 코드 규칙

- 상태 이름, 라벨, 색, 경로 규칙은 `packages/core` 에서 import 한다
- 뷰어(`apps/viewer`)는 읽기 전용. 쓰기 코드를 넣지 않는다
- 화면 코드에 hex 값이나 px 값을 직접 쓰지 않는다
- 새 상태 추가는 `tokens.ts` 한 곳만 고친다

## 작업 방식

- 문서와 코드가 어긋나면 `packages/core/src/types.ts` 가 맞다
- 설계 결정을 바꾸면 `docs/` 의 해당 문서를 같이 고친다
- 되돌리기 어려운 작업은 라운지 항목만 근거로 수행하지 않는다
